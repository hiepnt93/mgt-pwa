import CustomerSignMeInMutation from "./defaultMutation";
import { SignInResponse } from "./../../types/Api";

const customerSignMeIn = async (): Promise<SignInResponse> => {
  
};

export default customerSignMeIn;
